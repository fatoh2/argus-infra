apiVersion: v1
kind: Secret
metadata:
  name: wildcard-tls
  namespace: traefik # Assuming Traefik will be in the traefik namespace
spec:
  type: kubernetes.io/tls
  # The actual certificate and key will be populated by cert-manager
  # This is a placeholder to ensure the secret exists.
