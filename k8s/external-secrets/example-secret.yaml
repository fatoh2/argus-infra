apiVersion: v1
kind: Secret
metadata:
  name: example-external-secret
  namespace: default
type: Opaque
data:
  # This is a placeholder for a secret managed by External Secrets Operator or Doppler.
  # Actual secret data would be populated by the operator.
  # Example: base64 encoded value of 'my-secret-value'
  example_key: ""
