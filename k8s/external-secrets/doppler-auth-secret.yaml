apiVersion: v1
kind: Secret
metadata:
  name: doppler-auth
  namespace: external-secrets-operator
type: Opaque
stringData:
  # Replace with your actual Doppler service token:
  #   https://dashboard.doppler.com/workplace/project/<project>/config/<config>/tokens
  # This secret is a placeholder — apply it manually or via sealed secrets.
  token: "dp.st.example_placeholder_token"
