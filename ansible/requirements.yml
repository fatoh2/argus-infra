---
# Ansible Galaxy collection dependencies for the Argus homelab playbooks.
# Install with: ansible-galaxy collection install -r ansible/requirements.yml
collections:
  - name: community.general
  - name: ansible.posix
