# Argus Infra — Add to ~/.bashrc for Git Bash on Windows
# Copy and paste these lines into ~/.bashrc to make changes permanent

# Add local tools to PATH
export PATH="$HOME/.local/bin:$PATH"

# Kubernetes alias
alias k='kubectl'

# k3d alias
alias k3d='k3d'

# Helm alias
alias helm='helm'
